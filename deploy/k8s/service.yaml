apiVersion: v1
kind: Service
metadata:
  name: enterprise-agent-svc
  namespace: ai-services
spec:
  selector:
    app: enterprise-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
